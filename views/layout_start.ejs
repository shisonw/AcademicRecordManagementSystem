<!DOCTYPE html>
<html lang="en">
<head>
  <title>Student Online Scoring System</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <link rel="stylesheet" href="https://unpkg.com/bootstrap-table@1.18.2/dist/bootstrap-table.min.css">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css" crossorigin="anonymous">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
</head>


<body>
<div class="container-fluid">
    <div class="row flex-nowrap">
        <div class="col-sm-2 col-auto px-0 collapse collapse-horizontal overflow-hidden" id="sidebar">
            <% if (locals.type) { %>
            <div><a href="/home" style="text-decoration: none; color: white;"><button class="btn btn-success"><i class="fas fa-home"></i></button></a></div>
            <% } %>
            <div class="list-group border-0 text-center text-sm-start min-vh-100" id="sidebar-menu">
                <% if (locals.classes) { %> 
                    <% for (var c of classes) { %>
                        <% if (locals.classname && c.cname == classname) { %>
                        <a href="/class/<%= c._id %>" class="list-group-item border-0 d-inline-block text-truncate btn btn-primary" data-bs-parent="#sidebar"><span class="d-none d-sm-inline"><%= c.cname %></span></a>
                        <% } %>
                    <% } %>
                <% } %>
                <% if (locals.type && type == "admin") { %>
                    <a href="/teacher" class="list-group-item border-0 d-inline-block text-truncate btn btn-primary" data-bs-parent="#sidebar"><span class="d-none d-sm-inline">Teacher set class</span></a>
                <% } %> 
                <div class="fixed-bottom mx-auto">
                    <form class="mb-2" action="\">
                        <button type="submit" class="btn btn-primary">Logout</button>
                    </form>
                    <form class="mb-2" action="\changePassword">
                        <button type="submit" class="btn btn-primary">Change Password</button>
                    </form>
                </div>

            </div>
        </div>

        <div class="col-sm-auto col ps-md-2 pt-2 min-vh-100">
            <a href="#" data-bs-target="#sidebar" data-bs-toggle="collapse" class=" rounded-3 p-1 text-decoration-none"><i class="fas fa-ellipsis-h"></i></a>
        </div>
        
        <div id="content" class="container-fluid col-sm col border-start ps-md-2 pt-2">